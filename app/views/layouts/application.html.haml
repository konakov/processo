!!!
%html
  %head
    %title Processo
    = stylesheet_link_tag    "application", :media => "all"
    = javascript_include_tag "application"
    = csrf_meta_tags
  %body
    .row
      .container
        - logger.debug "#{flash}"
        - flash.each do |name,msg|
          .alert(class="alert-#{name == :notice ? 'succes' : 'error'}")
            = msg
        = yield
